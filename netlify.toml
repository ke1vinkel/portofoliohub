[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Exclude non-secret URLs and config variables from Netlify's secret scanner
  SECRETS_SCAN_OMIT_KEYS = "ALLOWED_FRAME_ANCESTORS,NEXT_PUBLIC_PORTAL_URL"

